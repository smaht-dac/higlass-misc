import register from 'higlass-register';

import ScannerResultTrack from './ScannerResultTrack';

register({
  name: 'ScannerResultTrack',
  track: ScannerResultTrack,
  config: ScannerResultTrack.config,
});
